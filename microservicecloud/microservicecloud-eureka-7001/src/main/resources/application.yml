#单机版
#server:
#  port: 7001
#  
#eureka:
#  server:
#    enable-self-preservation: false #关闭自我保护机制
#  instance:
#    hostname: localhost #eureka 服务端的实例名称
#  client:
#    register-with-eureka: false  #false表示 不向注册中心注册自己
#    fetch-registry: false #false 表示自己端是注册中心，我的职责就是维护服务实例，并不需要去检索服务
#    service-url:
#      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/   #设置与eureka server 交互地址查询服务和注册服务都需要依赖这个地址


#集群版      
server:
  port: 7001
  
eureka:
  instance:
    hostname: eureka7001.com  #eureka 服务端的实例名称
  client:
    register-with-eureka: false  #false表示 不向注册中心注册自己
    fetch-registry: false #false 表示自己端是注册中心，我的职责就是维护服务实例，并不需要去检索服务
    service-url:
      defaultZone: http://eureka7002.com:7002/eureka/,http://eureka7003.com:7003/eureka/      