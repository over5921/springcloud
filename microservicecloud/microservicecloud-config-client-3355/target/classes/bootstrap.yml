spring:
  cloud:
    config: 
#      name: microservicecloud-config-client #需要从github上读取的资源名称
      name: microservicecloud-config-dept-client
      profile: dev #本次访问的配置项
      label: master
      uri: http://config-3344.com:3344 #本微服务启动后先去找3344服务，通过springcloudconfig 获取GitHub的服务地址
    
    # 这是系统级的 